# services:
#   db:
#     image: postgres:15
#     container_name: postgres_db
#     restart: always
#     environment:
#       POSTGRES_USER: ${POSTGRES_USER}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#       POSTGRES_DB: ${POSTGRES_DB}
#     ports:
#       - "${DB_PORT}:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#       - ./infra/db/init:/docker-entrypoint-initdb.d
#     networks:
#       - backend

#   app:
#     build:
#       context: ./server
#       dockerfile: Dockerfile
#     container_name: go_app
#     depends_on:
#       - db
#     ports:
#       - "8080:8080"
#     networks:
#       - backend
#     environment:
#       - DB_HOST=db # データベースホスト名をサービス名で指定
#       - DB_PORT=${DB_PORT:-5432} # ポート番号も環境変数から取得するように修正 (connection.goに合わせて)
#       - DB_USER=${POSTGRES_USER} # .envから取得
#       - DB_PASSWORD=${POSTGRES_PASSWORD} # .envから取得
#       - DB_NAME=${POSTGRES_DB} # .envから取得
#     working_dir: /app_data  # アプリケーションの実行ディレクトリ
#     # volumes: # ボリュームマウントを削除し、Dockerfileのコピー内容のみを使用
#     #   - ./server:/app       # ホットリロード開発を有効にする (ソースコード用)
#     command: ./app          # 相対パスでアプリケーションを起動

# volumes:
#   pgdata:

# networks:
#   backend:
#     driver: bridge
